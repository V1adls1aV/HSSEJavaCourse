# Test Application Configuration
spring:
  application:
    name: homework-test
  flyway:
    enabled: false

server:
  port: 0  # Random port for testing

logging:
  level:
    me:
      vladislav:
        homework: DEBUG


