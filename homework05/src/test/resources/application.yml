# Test Application Configuration
spring:
  application:
    name: homework-test

server:
  port: 0  # Random port for testing
  tomcat:
    threads:
      min-spare: 10
      max: 50

logging:
  level:
    me:
      vladislav:
        homework: DEBUG

cache:
  expire-after-seconds: 300
  initial-capacity: 30
