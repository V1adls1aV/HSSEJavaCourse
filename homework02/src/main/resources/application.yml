# Application Configuration
spring:
  application:
    name: homework02

logging:
  level:
    me:
      vladislav:
        homework01: INFO

# OpenAPI/Swagger Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    use-root-path: true  # Wow, it's nice :)
    operationsSorter: method
    tagsSorter: alpha
